# ===================================
# RAGFlow-Plus 环境变量配置示例
# 复制此文件为 .env 并修改配置
# ===================================

# 文档引擎类型 (elasticsearch / infinity)
DOC_ENGINE=elasticsearch

# Docker Compose 配置
COMPOSE_PROFILES=${DOC_ENGINE}

# ============ Elasticsearch 配置 ============
STACK_VERSION=8.11.3
ES_HOST=es01
ES_PORT=1200
# 修改为你的 Elasticsearch 密码
ELASTIC_PASSWORD=your_es_password_here

# ============ Kibana 配置 ============
KIBANA_PORT=6601
KIBANA_USER=rag_flow
KIBANA_PASSWORD=your_kibana_password_here

# 内存限制 (8GB)
MEM_LIMIT=8073741824

# ============ MySQL 配置 ============
# 修改为你的 MySQL 密码
MYSQL_PASSWORD=your_mysql_password_here
MYSQL_HOST=mysql
MYSQL_DBNAME=rag_flow
MYSQL_PORT=5455

# ============ MinIO 配置 ============
# Docker Compose 环境中使用服务名称，本地调试改为 localhost
MINIO_HOST=minio
# 重要：修改为你的服务器公网 IP 或域名
MINIO_VISIT_HOST=your_server_ip_here
MINIO_CONSOLE_PORT=9001
MINIO_PORT=9000
MINIO_USER=rag_flow
# 修改为你的 MinIO 密码
MINIO_PASSWORD=your_minio_password_here

# ============ Redis 配置 ============
REDIS_HOST=redis
REDIS_PORT=6379
# 修改为你的 Redis 密码
REDIS_PASSWORD=your_redis_password_here

# ============ 服务配置 ============
SVR_HTTP_PORT=9380

# ============ Docker 镜像版本 ============
RAGFLOW_IMAGE=zstar1003/ragflowplus:v0.5.0
RAGFLOWPLUS_MANAGEMENT_WEB_IMAGE=zstar1003/ragflowplus-management-web:v0.5.0
RAGFLOWPLUS_MANAGEMENT_SERVER_IMAGE=zstar1003/ragflowplus-management-server:v0.5.0

# ============ 系统配置 ============
# 时区
TIMEZONE='Asia/Shanghai'

# HuggingFace 镜像地址（国内用户推荐使用）
HF_ENDPOINT=https://hf-mirror.com

# MacOS 用户设置为空字符串
MACOS=""

# 后端允许上传的最大文件大小 (10GB)
MAX_CONTENT_LENGTH=10737418240

# ============ 管理后台配置 ============
# 管理员用户名
MANAGEMENT_ADMIN_USERNAME=admin
# 修改为你的管理员密码
MANAGEMENT_ADMIN_PASSWORD=your_admin_password_here
# JWT 密钥（用于 token 加密，请修改为随机字符串）
MANAGEMENT_JWT_SECRET=your_jwt_secret_here
